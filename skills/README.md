# Skills Catalog

This repository can be used as an OpenChamber/OpenCode skills catalog.

Layout:

- `skills/<skill-id>/SKILL.md`

Each `SKILL.md` starts with YAML frontmatter (`name`, `description`, etc.) followed by the skill instructions.
